const fs = require('fs');
const input = fs.readFileSync("./input.txt").toString().trim().split("\n");
const [n,...stairs] = input
if(n % 2 === 0){
  //마지막을 제외하면 밟을 수 있는 계단은 홀수개
  
}
if(n % 2 !== 0){ 
 //마지막을 제외하면 밟을 수 있는 계단은 짝수개
}